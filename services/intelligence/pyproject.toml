[project]
name = "crisismesh-intelligence"
version = "0.3.0"
description = "Explainable six-hour disaster demand estimation and resource allocation"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1,<1",
]

[project.optional-dependencies]
dev = ["ruff>=0.12"]

[tool.setuptools.packages.find]
include = ["app*"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
